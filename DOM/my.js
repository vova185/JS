
const btn = document.querySelector('.btn');
const p = document.getElementById('myText');
const wordFields = document.querySelectorAll('.inp').values;
if (wordFields.length = 3) {}
    btn.addEventListener("click", () => {
        console.log(wordFields);
    });        